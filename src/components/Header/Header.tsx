import {HeaderTitle, PageHeader} from './Header.styles';

export const Header = () => {
    return(
        <PageHeader>
            <HeaderTitle>
                Busca de Linguagens
            </HeaderTitle>
        </PageHeader>
    )
}